# Copy to docker-compose.override.yml for local dev tweaks.
# Go services: use `make dev-api` / `make dev-ai` on host for fast iteration,
# or `docker compose up --build` to rebuild images after changes.

services:
  api:
    environment:
      LOG_LEVEL: debug

  ai:
    environment:
      LOG_LEVEL: debug
